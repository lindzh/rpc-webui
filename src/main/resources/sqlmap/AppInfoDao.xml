<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--  AutoGenerated by lindzh  2016年12月15日 16:52 -->
<mapper namespace="com.linda.rpc.webui.dao.AppInfoDao">

	<resultMap id="AppInfoResultMap" type="com.linda.rpc.webui.pojo.AppInfo">
		<id property="id" column="id"/>
		<result property="name" column="app_name"/>
		<result property="desc" column="app_desc"/>
		<result property="owner" column="app_owner"/>
		<result property="email" column="app_owner_email"/>
	</resultMap>
	
	<insert id="addAppInfo" useGeneratedKeys="true" keyProperty="id" parameterType="com.linda.rpc.webui.pojo.AppInfo">
		insert into app_info(app_name,app_desc,app_owner,app_owner_email)
		 values(#{name},#{desc},#{owner},#{email})
	</insert>
	
	<select id="getById" resultMap="AppInfoResultMap">
		select * from app_info where id=#{id}
	</select>
	
	<update id="updateById" parameterType="com.linda.rpc.webui.pojo.AppInfo">
		update app_info set app_name=#{obj.name},app_desc=#{obj.desc},app_owner=#{obj.owner},app_owner_email=#{obj.email}
		where id=#{obj.id}
	</update>
	
	<delete id="deleteById">
		delete from app_info where id=#{id}
	</delete>
	

	<select id="getListByAppName" resultMap="AppInfoResultMap">
		select * from app_info where
		app_name=#{name}
		order by id desc limit #{limit} offset #{offset}
	</select>
	
	<select id="getCountByAppName" resultType="long">
		select count(*) from app_info where 
		app_name=#{name}
	</select>
	
	
</mapper>